query ($id: Int!) {
	lesson: lms_findUniqueLesson(
		where: { id: $id }
	) {
		id
		name
		slug
		pos
		courseId
		moduleId
		html
		coverImage
		status
		createdBy
		createdAt
		updatedAt
		deleted
		course {
			id
			name
		}
	}
}