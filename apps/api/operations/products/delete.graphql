mutation ($id: Int!, $deleted: Boolean = true)
@rbac(requireMatchAll: [admin]) {
	product: lms_updateOneProduct(
		where: { id: $id }
		data: {
      deleted: { set: $deleted }
    }
	) {
		id
		deleted
	}
}