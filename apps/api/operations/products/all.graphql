query ($deleted: Boolean = false) {
	products: lms_findManyProduct() {
		id
		name
		description
		image
		price
		priceId
  	currency
		courses(where: { deleted: { equals: $deleted } }) {
			id
			name
			description
			image
			published
			createdBy
			createdAt
		}
	}
}