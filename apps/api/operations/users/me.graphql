query ($id: String! @fromClaim(name: USERID)) {
	me: lms_findUniqueUser(where: { id: $id }) {
		id
		customerId
		name
		firstName
		lastName
		email
		phone
		locale
		emailVerified
		image
		roles
	}
}