query ($userId: String! @fromClaim(name: USERID)) {
	users: lms_findManyMemberInCourse(
		where: { userId: { equals: $userId } }
	) {
		role
		course {
			id
			name
			description
			image
			published
			createdBy
			createdAt
			_count {
				modules
				lessons
				members
			}
		}
	}
}