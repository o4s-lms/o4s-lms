query ()
@rbac(requireMatchAll: [admin]) {
	users: site_findManyUser() {
		id
		uuid
		name
		given_name
		family_name
		middle_name
		nickname
		bio
		email
		gender
		locale
		email_verified
		picture
		roles
		member_of {
			course_id
			role
			course {
				name
				image
			}
		}
	}
}