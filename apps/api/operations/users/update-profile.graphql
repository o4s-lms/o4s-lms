mutation ($uuid: String!, $name: String!, $locale: String!)
@rbac(requireMatchAny: [user, admin]) {
	user: site_updateOneUser(
		where: {
			uuid: $uuid
			}
			data: {
				name: { set: $name }
				locale: { set: $locale }
				}
	) {
		id
		name
		locale
	}
}