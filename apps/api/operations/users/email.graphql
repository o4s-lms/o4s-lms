query ($email: String!)
@rbac(requireMatchAny: [user, admin]) {
	user: lms_findUniqueUser(where: { email: $email }) {
		id
		customerId
		name
		firstName
		lastName
		email
		phone
		locale
		emailVerified
		image
		roles
	}
}