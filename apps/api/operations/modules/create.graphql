mutation (
	$name: String!,
	$slug: String!,
	$userId: String @fromClaim(name: USERID)
	)
@rbac(requireMatchAll: [admin]) {
	createOnemodule: lms_createOneModule(
		data: {
			name: $name
			slug: $slug
		}
	) {
		id
		name
		slug
	}
}