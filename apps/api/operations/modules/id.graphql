query ($id: Int!, $deleted: Boolean = false) {
	module: lms_findUniqueModule(
		where: { id: $id }
	) {
		id
		name
		slug
		pos
		courseId
		createdBy
		createdAt
		lessons(
			where: { deleted: { equals: $deleted } }
			orderBy: { pos: asc }
		) {
			id
			name
			slug
			pos
			status
			courseId
			moduleId
			createdBy
			createdAt
			updatedAt
		}
	}
}