query ($locale: String!) {
	courses: site_findManyCourses(
		where: { locale: { equals: $locale } }
		orderBy: { order: asc }
	) {
		id
		thumbnail
		price
		image
		title
		description
		slug
		include
		html
		order
		locale
	}
}