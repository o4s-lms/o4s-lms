query ($locale: String!) {
	courses: site_findManyCourse(
		where: { locale: { equals: $locale } }
		orderBy: { order: asc }
	) {
		id
		thumbnail
		price
		image
		title
		description
		slug
		include
		html
		order
		locale
	}
}