query ($locale: String!) {
	products: site_findManyProduct(
		where: { locale: { equals: $locale } }
		orderBy: { order: asc }
	) {
		id
		thumbnail
		price
		image
		title
		description
		slug
		include
		html
		order
		locale
	}
}